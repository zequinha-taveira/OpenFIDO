name: "OpenFIDO CodeQL Config"

disable-default-queries: false

queries:
  - uses: security-extended
  - uses: security-and-quality

paths:
  - src

paths-ignore:
  - build
  - build_*
  - .pio
  - tests/mock_*.c
  - "**/*.md"

query-filters:
  - exclude:
      id: cpp/path-injection
